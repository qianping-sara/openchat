# Generate a random secret: https://generate-secret.vercel.app/32 or `openssl rand -base64 32`
AUTH_SECRET=****

# The following keys below are automatically created and
# added to your environment when you deploy on Vercel

# Instructions to create an AI Gateway API key here: https://vercel.com/ai-gateway 
# API key required for non-Vercel deployments
# For Vercel deployments, OIDC tokens are used automatically
# https://vercel.com/ai-gateway
AI_GATEWAY_API_KEY=****


# Instructions to create a Vercel Blob Store here: https://vercel.com/docs/vercel-blob
BLOB_READ_WRITE_TOKEN=****

# Instructions to create a PostgreSQL database here: https://vercel.com/docs/postgres
POSTGRES_URL=****


# Instructions to create a Redis store here:
# https://vercel.com/docs/redis
REDIS_URL=****

# DeepSeek API Key (optional - only needed if using direct DeepSeek API)
# Get your API key from: https://platform.deepseek.com/api_keys
DEEPSEEK_API_KEY=****

# --- MCP (optional) ---
# Tavily: set TAVILY_API_KEY to enable web search MCP
# TAVILY_API_KEY=****

# --- Neo4j (optional) ---
# Neo4j is now integrated using neo4j-driver (not MCP) for better serverless performance.
# Provides READ-ONLY access to Neo4j graph database for AI queries.
# Get Neo4j credentials from: https://neo4j.com/cloud/aura/ or your self-hosted instance
#
# URI formats:
#   - neo4j+s://   (encrypted with system CA certificates - recommended for Aura)
#   - neo4j+ssc:// (encrypted with self-signed certificate)
#   - neo4j://     (unencrypted - only for local development)
#   - bolt+s://    (legacy encrypted)
#   - bolt://      (legacy unencrypted)
#
# NEO4J_URI=neo4j+s://your-instance.databases.neo4j.io
# NEO4J_USERNAME=neo4j
# NEO4J_PASSWORD=****
# NEO4J_DATABASE=neo4j  # Optional, defaults to "neo4j"

# Legacy Neo4j MCP (DEPRECATED - disabled by default)
# The MCP approach has been replaced with direct neo4j-driver for better performance.
# Only enable if you specifically need MCP features.
# NEO4J_MCP_ENABLED=false
